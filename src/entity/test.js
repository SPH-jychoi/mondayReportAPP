export default class Group {
    constructor(obj) {
      this.id = obj.id;
      this.title = obj.title;
    }

    getId(){
      return this.id;
    }
    getTitle(){
      return this.title;
    }
  }

  export class item {
    constructor(obj){
      this.name = obj.name;
      this.
    }
  }